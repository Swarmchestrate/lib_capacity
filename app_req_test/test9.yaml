app1:
  - host:
      node_filter:
        $and:
          - $equal:
              - $get_property: [host, num-cpus]
              - 2
          - $equal:
              - $get_property: [host, mem-size]
              - 2
        # num-cpus == 2 and mem-size == 2
