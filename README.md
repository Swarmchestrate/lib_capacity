# lib_capacity
library to handle capacity registry

## Python library package

This project is packaged as `swch_capreg`.

- Main class: `swch_capreg.SwChCapacityRegistry`
- Helper classes: `swch_capreg.AppReq`, `swch_capreg.ResCap`

### Placeholder for method list

Edit `swch_capreg/methods.py` and populate:

- `METHODS: list[str] = []`

with the method names you want to define or expose.
